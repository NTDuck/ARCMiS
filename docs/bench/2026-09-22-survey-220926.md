# Survey 220926 — methodology × model sweep

Status: RUNNING (~70 of 180 attempts done). This report carries the
current numbers from `experiments/220926/results/aggregated.yml`;
rerun `python3 scripts/aggregate-survey.py` and this file gets a
final pass when the sweep completes.

## Executive summary

Pending full sweep. Interim: the 2B model (minicpm5-2b) completed all
60 of its cells with zero toolchain-verified successes — it cannot
format ollama tool-call arguments reliably enough to drive any of the
three agent pipelines. The 27B qwen3.8 block started with 2/10
toolchain-verified monolith successes on the smallest project (fft).

## Survey design

- Grid: 4 projects x 3 methods x 3 models x 5 reps = 180 cells, one
  harness process per cell, strictly sequential.
- Projects (ReCodeAgent dataset, crust split, C to Rust): `fft` (233
  C LOC), `totp` (443), `cjson` (1101), `expr` (1110).
- Methods:
  - `monolith` — one ReAct agent (write + bash tools) plus a validator
    agent; 14-turn budget.
  - `ledger` — manager-worker scaffold with shared workspace files
    (plan.md, tasks.json, notes.md), brainstorm-then-manage flow per
    arXiv:2608.26480 section 3.1; 14-turn manager budget.
  - `recode` — analyzer, planner, translator, validator loop per
    arXiv:2604.07341 Algorithm 1; 40 turns per phase agent.
- Models (all ollama, one at a time, offloaded between every run):
  - `openbmb/minicpm5-2b:q8_0` (2B)
  - `qwen3.8:27b-mtp-q4_K_M` (27B official)
  - `smtek/Swift-Qwen3.8-27B:dflash2` (27B fine-tune)
- Excluded: `SparkLLM/Spark-X2.5-4B` — its `spark2_5` GGUF
  architecture fails to load in the local daemon.
- Scoring: the toolchain decides. After each run the harness reruns
  `cargo build` and `cargo test` in the produced workspace; agent
  self-reports are recorded but never trusted.
- VRAM discipline: `--offload` unloads every resident model before and
  after each run, verified by polling `/api/ps` until empty.

## Interim results (70 attempts)

| Slice | Attempts | Compiled | Scored | Pass rate |
| --- | --- | --- | --- | --- |
| minicpm5-2b (all) | 60 | 0 | 0 | 0.00 |
| qwen3.8-27B so far | 10 | 2 | 2 | 0.00 (tests ran, 0 of reported pass-rate fraction) |
| monolith so far | 25 | 2 | 2 | 0.00 |
| ledger so far | 25 | 0 | 0 | — |
| recode so far | 20 | 0 | 0 | — |
| fft so far | 25 | 2 | 2 | 0.00 |
| cjson/totp/expr so far | 45 | 0 | 0 | — |

The two successes are `fft/monolith/qwen3.8` reps 2 and 3: compiled
workspaces, cargo test green (1 test), verified by the evaluator
rerun, results and traces persisted per ADR 0020 layout.

## Failure analysis (minicpm5-2b block, 60 attempts)

| Class | Count | Share |
| --- | --- | --- |
| max_turns (budget exhausted mid-translation) | 31 | 52% |
| invalid tool call arguments (daemon HTTP 500) | 14 | 23% |
| other (json parse errors, empty responses) | 15 | 25% |

Reading: the 2B model fails tool-call argument formatting at a rate
that makes agent-driven translation unreachable; most runs die either
exhausting turns on malformed calls or on daemon-side 500s rejecting
malformed JSON arguments.

## Conclusions

Pending full sweep.
