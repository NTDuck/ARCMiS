[package]
name = "ulidgen"
version = "0.1.0"
edition = "2021"
description = "generate or tag lines with ULID"
license = "Unlicense"

# keep this crate out of any enclosing cargo workspace
[workspace]

[dependencies]
getrandom = "0.2"
