[package]
name = "ulidgen"
version = "0.1.0"
edition = "2021"
description = "generate or tag lines with ULID (Universally Unique Lexicographically Sortable Identifier)"
license = "Unlicense"

# Keep this package independent of any enclosing Cargo workspace.
[workspace]

[dependencies]
rand = "0.9"
