[package]
name = "ulidgen"
version = "0.1.0"
edition = "2021"

# Keep this package out of any enclosing cargo workspace.
[workspace]

[dependencies]
clap = { version = "4", features = ["derive"] }
getrandom = "0.2"
