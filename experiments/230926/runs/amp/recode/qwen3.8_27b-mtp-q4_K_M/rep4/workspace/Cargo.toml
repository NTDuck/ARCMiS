[package]
name = "amp"
version = "0.0.1"
edition = "2021"
description = "Abstract Message Protocol"
license = "MIT"

# Keep this package standalone even when nested inside another cargo workspace.
[workspace]

[dependencies]
